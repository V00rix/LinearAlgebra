//package common
//
//class LinearMultiply[ScalarType, VectorType](val d: ((ScalarType, LinearSpace[ScalarType, VectorType]#Vector)) => LinearSpace[ScalarType, VectorType]#Vector)
//  extends Function[(ScalarType, LinearSpace[ScalarType, VectorType]#Vector), LinearSpace[ScalarType, VectorType]#Vector](d) {
//
//
//  // functions should:
//  /*
//  x add y === y add x
//  x add zero === zero
//  x add (y add z) === (x add y) add z
//  exists y: x add y = zero (function?)
//
//  exists one : x multiply one = x
//  alpha * (betta * x)  = (alpha * betta) * x
//
//  alpha * (x + y) = alpha * x + alpha * y
//  (alpha + betta) * x = alpha * x + betta * x
//
//  how to check? spec
//   */
//}
